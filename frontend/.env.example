# Copy to .env.local before running `npm run dev`.
#
# Base URL of the FastAPI inference service started from backend/.
# NEXT_PUBLIC_ variables are inlined at build time — restart the dev server
# after changing this.
NEXT_PUBLIC_API_URL=http://localhost:8000
